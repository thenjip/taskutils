type
  AbsoluteDir* = string
  AbsoluteFile* = string

  RelativeDir* = string
  RelativeFile* = string

  DirPath* = string
    ## Can be absolute or relative depending on the context.
  FilePath* = string
    ## Can be absolute or relative depending on the context.
